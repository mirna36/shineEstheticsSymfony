{% extends 'base.html.twig' %}
{% block body %}

    {% block header %}
        {{ parent() }}
    {% endblock %}
    <div class="titre">
        {{ titre }}
    </div>
    {% block section %}

<section id="home-icones">
    <div class="container">
        <div class="row">
            {% for articlesPrestations in produits%}
                <div id="soins" class=" col-md-3 text-center ">
                    <img src="{{ asset('images/products/') }}{{ articlesPrestations.nomPhoto}}" class="img-product " alt="{{ articlesPrestations.nomPhoto }}">
                    <h3>{{ articlesPrestations.libelle }}</h3><br>
                    <button type="button" class="btn-danger">Detail</button>
                    <button type="button" class="btn-danger">Ajouter au panier</button><br><br><br>


                </div>
            {% endfor %}

            {% endblock %}


    {% block footer %}
        {{ parent() }}
    {% endblock %}
    {% endblock %}