{% extends 'base.html.twig' %}

{% block title %}{{ titre_page }}!{% endblock %}

{% block body %}
    {% block header %}
        {{ parent() }}

    {% endblock %}
<div class="container mt-4">
    <div class="formLogin-card mx-auto ">
        <div>
             <i class="fas fa-user-lock"></i>
            <h1 class="h3 mb-3 font-weight-normal">Merci de créer votre compte</h1>
        </div>
        {{ form_start(registrationForm) }}
        {{ form_widget(registrationForm) }}

        <button class="btn btn-lg btn-danger" type="submit">
            S'enregistrer
        </button>
    <br>
    <br>
        <P>vous avez déjà un compte? connectez-vous <a href="{{ path('app_login') }}">ici</a></P>
        {{ form_end(registrationForm) }}
    </div>

</div>

    {% block footer %}
        {{ parent() }}
    {% endblock %}
    {% block javascripts %}
        {{ parent() }}
    {% endblock %}
{% endblock %}
